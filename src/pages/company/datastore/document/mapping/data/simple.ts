const defaultFn = `
  /**
   * 生成数据
   * @constructor
   * @param {row} 当前行的数据，一般来说是一个map类型
   * @return {any} 
   */
  function build(row) {
      let val;
      // TODO: 
      // 做一些处理，例如计算，拼接等简单处理
      return val;
  }
`;
